# shader_LUX_replacer
This is a simple script that adds the LUX_ prefix to a selected VMTs' shader.
Current mainly supported LUX Shaders are VertexLitGeneric and UnlitGeneric.

Use it on a model as a Rig.

Will also clear the material cache for instant material reload
